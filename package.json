{
  "name": "latvian-transcription",
  "version": "1.0.0",
  "description": "Latvian Audio Transcription System",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "dev": "cd frontend && npm start"
  },
  "dependencies": {
    "formidable": "^3.5.1",
    "@anthropic-ai/sdk": "^0.27.3",
    "node-fetch": "^2.7.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
